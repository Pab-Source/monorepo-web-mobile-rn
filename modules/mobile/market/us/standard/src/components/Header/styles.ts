import { StyleSheet } from 'react-native';

export const makeStyles = () =>
  StyleSheet.create({
    container: {
      alignItems: 'flex-start',
      paddingBottom: 27,
    },
    textClose: {
      fontSize: 20,
    },
  });
